/**
 * Application Configuration Template
 *
 * Copy this file to config.js and fill in your values.
 * The config.js file is excluded from version control.
 */

window.APP_CONFIG = {
    // Password used to derive encryption key for API key storage
    // Generate a unique, secure password (recommended: 32+ characters with mixed case, numbers, symbols)
    // Example: openssl rand -base64 32
    API_KEY_ENCRYPTION_PASSWORD: 'CHANGE_THIS_TO_A_SECURE_PASSWORD'
};
